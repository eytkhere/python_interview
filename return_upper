# array -> return upper case

base=>sizeof(arr)==0

	def upper_array(arr):
		if(len(arr)==0):
			return []
		else:
			return [arr[0].upper]+upper_array(arr[1:])
arr=['abc', 'def', 'ghi']
print(upper_array(arr))

